@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<ul class="navbar-nav">
    <!-- Company Dashboard -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "CompanyDashboard" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "CompanyDashboard")">
            <i class="material-symbols-rounded opacity-5">dashboard</i>
            <span class="nav-link-text ms-1">Dashboard</span>
        </a>
    </li>

    <!-- Company Management -->
    <li class="nav-item mt-3">
        <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">Company Management</h6>
    </li>

    <!-- Company Users -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "CompanyUsers" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "CompanyUsers")">
            <i class="material-symbols-rounded opacity-5">group</i>
            <span class="nav-link-text ms-1">Company Users</span>
        </a>
    </li>

    <!-- Company Settings -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "CompanySettings" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "CompanySettings")">
            <i class="material-symbols-rounded opacity-5">business</i>
            <span class="nav-link-text ms-1">Company Settings</span>
        </a>
    </li>

    <!-- Content Management -->
    <li class="nav-item mt-3">
        <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">Content Management</h6>
    </li>

    <!-- Company Documents -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "CompanyDocuments" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "CompanyDocuments")">
            <i class="material-symbols-rounded opacity-5">description</i>
            <span class="nav-link-text ms-1">Company Documents</span>
        </a>
    </li>

    <!-- Company Collections -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "CompanyCollections" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "CompanyCollections")">
            <i class="material-symbols-rounded opacity-5">folder</i>
            <span class="nav-link-text ms-1">Company Collections</span>
        </a>
    </li>

    <!-- My Content -->
    <li class="nav-item mt-3">
        <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">My Content</h6>
    </li>

    <!-- My Documents -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Documents" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "Documents")">
            <i class="material-symbols-rounded opacity-5">description</i>
            <span class="nav-link-text ms-1">My Documents</span>
        </a>
    </li>

    <!-- My Collections -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Collections" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "Collections")">
            <i class="material-symbols-rounded opacity-5">folder</i>
            <span class="nav-link-text ms-1">My Collections</span>
        </a>
    </li>

    <!-- Tools -->
    <li class="nav-item mt-3">
        <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">Tools</h6>
    </li>

    <!-- AI Chat -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Chat" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "Chat")">
            <i class="material-symbols-rounded opacity-5">chat</i>
            <span class="nav-link-text ms-1">AI Chat</span>
        </a>
    </li>

    <!-- Search -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Search" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "Search")">
            <i class="material-symbols-rounded opacity-5">search</i>
            <span class="nav-link-text ms-1">Search</span>
        </a>
    </li>

    <!-- Account -->
    <li class="nav-item mt-3">
        <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">Account</h6>
    </li>

    <!-- Profile -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Profile" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Index", "Profile")">
            <i class="material-symbols-rounded opacity-5">person</i>
            <span class="nav-link-text ms-1">Profile</span>
        </a>
    </li>

    <!-- Settings -->
    <li class="nav-item">
        <a class="nav-link @(currentController == "Settings" ? "active bg-gradient-primary" : "text-dark")" 
           href="@Url.Action("Account", "Settings")">
            <i class="material-symbols-rounded opacity-5">settings</i>
            <span class="nav-link-text ms-1">Settings</span>
        </a>
    </li>

    <!-- Logout -->
    <li class="nav-item">
        <div class="nav-link text-dark cursor-pointer" onclick="logout()">
            <i class="material-symbols-rounded opacity-5">logout</i>
            <span class="nav-link-text ms-1">Logout</span>
        </a>
    </li>
</ul>

<script>
function logout() {
    if (confirm('Are you sure you want to logout?')) {
        window.location.href = '@Url.Action("Logout", "Auth")';
    }
}
</script>